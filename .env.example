# ===========================================
# AgentZero Environment Configuration
# ===========================================
# Copy this file to .env and fill in your values.
# NEVER commit .env with real credentials.

# --- LLM Provider ---
# Options: "ollama" (local) or "cloudflare" (remote)
LLM_PROVIDER=ollama

# --- Ollama Configuration (local LLM) ---
OLLAMA_MODEL=mistral-nemo:12b
OLLAMA_API_URL=http://localhost:11434/api/generate
OLLAMA_EMBEDDINGS_API_URL=http://localhost:11434/api/embeddings

# --- Cloudflare Workers AI Configuration ---
CLOUDFLARE_ACCOUNT_ID=your_account_id_here
CLOUDFLARE_API_TOKEN=your_api_token_here
CLOUDFLARE_TEXT_MODEL=@cf/mistralai/mistral-small-3.1-24b-instruct
CLOUDFLARE_EMBEDDING_MODEL=@cf/baai/bge-base-en-v1.5

# --- WhatsApp Cloud API ---
WHATSAPP_PHONE_NUMBER_ID=your_phone_number_id_here
WHATSAPP_ACCESS_TOKEN=your_access_token_here
WHATSAPP_VERIFY_TOKEN=your_verify_token_here

# --- JWT Authentication ---
# Generate a random secret: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=your_random_secret_here
JWT_EXPIRY_HOURS=24
ADMIN_USERNAME=admin
# Generate hash: start server, call GET /auth/hash-password?password=yourpassword, then paste here
ADMIN_PASSWORD_HASH=your_bcrypt_hash_here
