version: '3.8'

services:
  agentzero-api:
    build: .
    container_name: agentzero_api
    restart: always # Ideal for Azure App Service to ensure background tasks keep running
    ports:
      - "8000:8000"
    env_file:
      - .env
